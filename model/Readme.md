This will include **UnitLib** folder only which contains libraries from which SWC is created.
At this level, this folder can also contain SWC model file and SLDD, which can be used for code generation

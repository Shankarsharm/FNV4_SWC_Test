name: Syncing the repos
on:
  push:
    branches:
      - 'Jenkins'
  #  paths:  
  #   - 'sync/repo/Readme.md'
  workflow_dispatch:
jobs:
  Sync:
    runs-on: ubuntu-latest
    steps:
      - name: Get Code
        uses: actions/checkout@v3
      - name: Install yq
        run: |
          wget -qO /usr/local/bin/yq https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64
          chmod a+x /usr/local/bin/yq
          yq --version
      - name: Run Shell Script
        id: capture
        run: |
          git config --global user.email "shankar.sharma@gmail.com"
          git config --global user.name "Shankarsharm"
          cd ./.github/runfiles
          ls
          pwd
          chmod +x sync.sh
          ./sync.sh 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 FNV4_SWC_Test
